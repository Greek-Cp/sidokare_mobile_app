import 'package:flutter/material.dart';

class ModelAccount {
  String? nama;
  String? username;
  String? email;
  String? noTelepon;
  String? password;
  ModelAccount(
      this.nama, this.username, this.email, this.noTelepon, this.password);
}
