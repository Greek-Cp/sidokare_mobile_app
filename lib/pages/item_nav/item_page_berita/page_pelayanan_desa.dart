import 'package:flutter/material.dart';

class pagePelayananDesa extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: Center(
        child: Text("Pelayanan Desa"),
      ),
    );
  }
}
