import 'package:flutter/material.dart';

class pagePemberdayaanMasyarakat extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      body: Center(
        child: Text("pagePemberdayaanMasyarakat"),
      ),
    );
  }
}
